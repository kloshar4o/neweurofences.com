@extends('admin.app')@section('content')    @include('admin.breadcrumbs')    @php($alias = request()->segment(4))    @include('admin.list-elements', [        'actions' => [            myTrans('All') => "",            myTrans('Create') => "form/$lang_id/",        ]    ])    <div id="{{$blade}}">        <table-template @edit-click="editClick"                        :filter-key="searchQuery"                        :rows="gridData"                        :tab-code="'code'">        </table-template>{{--        <div :id="color.id" v-for="color in colors">            <div>                <a :href="color.url"                   :class="'hexagon ral_' + color.ral"                   :title="'RAL_' + color.ral">                    <div class="ratio-1-1" :style="'background-color: ' + color.hex"></div>                    <div class="item-page-text" v-text="color.name"></div>                </a>            </div>            <div v-text="color.ral"></div>            <div v-text="color.hex"></div>            <div class="edit-links">                <a v-for="color_lang in color.lang_list"                   :href="color_lang.url"                   :class="color_lang.active && 'active'">                    <b v-text="color_lang.lang"></b>                </a>            </div>        </div>--}}        <div class="empty-response" v-show="!gridData.data.length">{{trans('variables.list_is_empty')}}</div>    </div>    <div id="loader-gif" class="loader-list"></div>    @push('scripts')        <script>            const gridData = @json($gridData);            new Vue({                'el': '#{{$blade}}',                data(){                    return {                        gridData : gridData,                        searchQuery: ''                    }                },                methods:{                    editClick(ha){                        console.log(ha);                    }                }            })        </script>    @endpush@stop